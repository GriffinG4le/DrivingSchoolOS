# Frontend (Vite)

## Environment
The frontend expects these env vars (see `.env.example` in the repo root):
- `VITE_SUPABASE_URL`: Supabase project URL (starts with `https://`)
- `VITE_SUPABASE_ANON_KEY`: Supabase anon public key

## Run

```bash
npm install
npm run dev
```
